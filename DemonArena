-- =====================================
--   DEMON ARENA - LILYVREDHAWK
--   BY LILYEYES - MOBILE FRIENDLY GUI
-- =====================================

local replicatedStorage = game:GetService("ReplicatedStorage")
local player = game:GetService("Players").LocalPlayer
local runService = game:GetService("RunService")
local userInputService = game:GetService("UserInputService")
local tweenService = game:GetService("TweenService")

-- ========== AMBIL REMOTE ==========
local function findRemote(name)
    for _, v in pairs(replicatedStorage:GetDescendants()) do
        if v:IsA("RemoteEvent") or v:IsA("RemoteFunction") then
            if string.find(string.lower(v.Name), string.lower(name)) then
                return v
            end
        end
    end
    return nil
end

local clickRemote = findRemote("ReqClickTrain")
local speedRemote = findRemote("ReqUpdateTrainSpeed")
local equipRemote = findRemote("ReqApplyEquipTrainMethod")
local unequipRemote = findRemote("ReqApplyUnequipTrainMethod")

-- ========== AMBIL MODULE ==========
local trainModule = nil
local function getTrainModule()
    if trainModule then return trainModule end
    local script = replicatedStorage:FindFirstChild("Script")
    if script then
        local train = script:FindFirstChild("TrainSystem")
        if train then
            local service = train:FindFirstChild("TrainSystemService")
            if service then
                local success, module = pcall(require, service)
                if success then
                    trainModule = module
                    return module
                end
            end
        end
    end
    return nil
end
trainModule = getTrainModule()

-- ========== FUNGSI ==========
local function cachedClick()
    if clickRemote then
        pcall(function()
            if clickRemote:IsA("RemoteFunction") then
                clickRemote:InvokeServer()
            else
                clickRemote:FireServer()
            end
        end)
    end
end

local function cachedUpdateSpeed(speed)
    if speedRemote then
        pcall(function()
            if speedRemote:IsA("RemoteFunction") then
                speedRemote:InvokeServer(speed or 999)
            else
                speedRemote:FireServer(speed or 999)
            end
        end)
    end
end

local function cachedEquip(methodId)
    if equipRemote then
        pcall(function()
            if equipRemote:IsA("RemoteFunction") then
                equipRemote:InvokeServer(methodId)
            else
                equipRemote:FireServer(methodId)
            end
        end)
    end
end

local function cachedUnequip()
    if unequipRemote then
        pcall(function()
            if unequipRemote:IsA("RemoteFunction") then
                unequipRemote:InvokeServer()
            else
                unequipRemote:FireServer()
            end
        end)
    end
    if trainModule and trainModule.requestUnequipTrainMethod then
        pcall(trainModule.requestUnequipTrainMethod)
    end
end

local function unequipTrain()
    cachedUnequip()
    pcall(function()
        local char = player.Character
        if char then
            local humanoid = char:FindFirstChildOfClass("Humanoid")
            if humanoid then
                local animator = humanoid:FindFirstChildOfClass("Animator")
                if animator then
                    for _, track in pairs(animator:GetPlayingAnimationTracks()) do
                        track:Stop()
                    end
                end
            end
        end
    end)
end

local autoUnequipConnected = false
local function setupAutoUnequip()
    if autoUnequipConnected then return end
    pcall(function()
        local char = player.Character or player.CharacterAdded:Wait()
        local humanoid = char:WaitForChild("Humanoid")
        humanoid:GetPropertyChangedSignal("MoveDirection"):Connect(function()
            if humanoid.MoveDirection.Magnitude > 0.1 then
                unequipTrain()
            end
        end)
        autoUnequipConnected = true
    end)
end

local function quickMemoryClean()
    pcall(function()
        collectgarbage("collect")
        collectgarbage("step")
    end)
end

-- ========== BYPASS ==========
local bypassActive = false
local bypassConnection = nil
local currentSpeed = 999
local decayDisabled = false

local function activateBypass(speed)
    if not trainModule then return end
    speed = speed or currentSpeed
    currentSpeed = speed
    
    if trainModule.getClickTrainSpeed then
        trainModule.getClickTrainSpeed = function() return speed end
    end
    
    if trainModule.onClickAccelerate then
        trainModule.onClickAccelerate = function()
            local remote = replicatedStorage:FindFirstChild("Remote")
            if remote then
                local ts = remote:FindFirstChild("TrainSystem")
                if ts then
                    local update = ts:FindFirstChild("ReqUpdateTrainSpeed")
                    if update then pcall(function() update:InvokeServer(speed) end) end
                end
            end
        end
    end
    
    local remote = replicatedStorage:FindFirstChild("Remote")
    if remote then
        local ts = remote:FindFirstChild("TrainSystem")
        if ts then
            local update = ts:FindFirstChild("ReqUpdateTrainSpeed")
            if update then
                local oldInvoke = update.InvokeServer
                update.InvokeServer = function(self, spd)
                    return oldInvoke(self, speed)
                end
            end
        end
    end
    
    if trainModule.startDecayCheck and not decayDisabled then
        trainModule.startDecayCheck = function() return end
        decayDisabled = true
    end
    
    if not bypassConnection then
        local remote = replicatedStorage:FindFirstChild("Remote")
        if remote then
            local ts = remote:FindFirstChild("TrainSystem")
            if ts then
                local update = ts:FindFirstChild("ReqUpdateTrainSpeed")
                if update then
                    bypassConnection = runService.Heartbeat:Connect(function()
                        if bypassActive then
                            pcall(function() update:InvokeServer(speed) end)
                        end
                    end)
                end
            end
        end
    end
end

local function deactivateBypass()
    bypassActive = false
    if bypassConnection then
        bypassConnection:Disconnect()
        bypassConnection = nil
    end
end

-- ========== LOOP ==========
local isRunning = false
local loopConnection = nil
local frameCount = 0

local function startLoop()
    if isRunning then return end
    isRunning = true
    
    if not bypassActive then
        bypassActive = true
        activateBypass(currentSpeed)
    end
    
    if autoUnequipEnabled then
        setupAutoUnequip()
    end
    
    loopConnection = runService.Heartbeat:Connect(function()
        if isRunning then
            frameCount = frameCount + 1
            for i = 1, 10 do
                cachedClick()
            end
            cachedUpdateSpeed(currentSpeed)
            if frameCount % 100 == 0 then
                quickMemoryClean()
            end
        end
    end)
end

local function stopLoop()
    isRunning = false
    if loopConnection then
        loopConnection:Disconnect()
        loopConnection = nil
    end
    unequipTrain()
end

-- ========== GUI MOBILE FRIENDLY ==========
local gui = Instance.new("ScreenGui")
gui.Name = "LilyVRedHawk"
gui.Parent = player:WaitForChild("PlayerGui")
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Main Frame dengan ukuran lebih besar untuk mobile
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 280, 0, 370)
mainFrame.Position = UDim2.new(0.5, -140, 0.5, -185)
mainFrame.BackgroundColor3 = Color3.fromRGB(10, 0, 10)
mainFrame.BackgroundTransparency = 0.08
mainFrame.BorderSizePixel = 2
mainFrame.BorderColor3 = Color3.fromRGB(200, 0, 50)
mainFrame.ClipsDescendants = true
mainFrame.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)
corner.Parent = mainFrame

-- Glow
local glow = Instance.new("Frame")
glow.Size = UDim2.new(1, 10, 1, 10)
glow.Position = UDim2.new(0, -5, 0, -5)
glow.BackgroundColor3 = Color3.fromRGB(255, 0, 50)
glow.BackgroundTransparency = 0.9
glow.BorderSizePixel = 0
glow.Parent = mainFrame

local glowCorner = Instance.new("UICorner")
glowCorner.CornerRadius = UDim.new(0, 10)
glowCorner.Parent = glow

-- Header (lebih tinggi untuk mobile)
local header = Instance.new("Frame")
header.Size = UDim2.new(1, 0, 0, 45)
header.BackgroundColor3 = Color3.fromRGB(200, 0, 30)
header.BackgroundTransparency = 0.1
header.BorderSizePixel = 0
header.Parent = mainFrame

local titleLogo = Instance.new("TextLabel")
titleLogo.Size = UDim2.new(1, -80, 1, 0)
titleLogo.Position = UDim2.new(0, 10, 0, 0)
titleLogo.Text = "✦ LilyVRedHawk ✦"
titleLogo.TextColor3 = Color3.fromRGB(255, 50, 50)
titleLogo.BackgroundTransparency = 1
titleLogo.Font = Enum.Font.GothamBold
titleLogo.TextSize = 16
titleLogo.TextScaled = true
titleLogo.Parent = header

-- Button container di header
local headerBtns = Instance.new("Frame")
headerBtns.Size = UDim2.new(0, 60, 1, 0)
headerBtns.Position = UDim2.new(1, -65, 0, 0)
headerBtns.BackgroundTransparency = 1
headerBtns.Parent = header

-- Minimize Button
local minBtn = Instance.new("TextButton")
minBtn.Size = UDim2.new(0, 25, 0, 25)
minBtn.Position = UDim2.new(0, 0, 0.5, -12.5)
minBtn.Text = "─"
minBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
minBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 30)
minBtn.BackgroundTransparency = 0.3
minBtn.Font = Enum.Font.GothamBold
minBtn.TextSize = 16
minBtn.BorderSizePixel = 1
minBtn.BorderColor3 = Color3.fromRGB(255, 50, 50)
minBtn.Parent = headerBtns

local minCorner = Instance.new("UICorner")
minCorner.CornerRadius = UDim.new(0, 4)
minCorner.Parent = minBtn

-- Close Button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 25, 0, 25)
closeBtn.Position = UDim2.new(1, -25, 0.5, -12.5)
closeBtn.Text = "✕"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
closeBtn.BackgroundTransparency = 0.3
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 14
closeBtn.BorderSizePixel = 1
closeBtn.BorderColor3 = Color3.fromRGB(255, 50, 50)
closeBtn.Parent = headerBtns

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 4)
closeCorner.Parent = closeBtn

closeBtn.MouseButton1Click:Connect(function()
    stopLoop()
    deactivateBypass()
    gui:Destroy()
end)

-- Content Frame
local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1, 0, 1, -45)
contentFrame.Position = UDim2.new(0, 0, 0, 45)
contentFrame.BackgroundTransparency = 1
contentFrame.Parent = mainFrame

-- Divider
local div = Instance.new("Frame")
div.Size = UDim2.new(0.9, 0, 0, 1)
div.Position = UDim2.new(0.05, 0, 0, 0)
div.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
div.BackgroundTransparency = 0.3
div.BorderSizePixel = 0
div.Parent = contentFrame

-- Status Row
local statusRow = Instance.new("Frame")
statusRow.Size = UDim2.new(1, -20, 0, 30)
statusRow.Position = UDim2.new(0, 10, 0, 5)
statusRow.BackgroundTransparency = 1
statusRow.Parent = contentFrame

local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(0.5, 0, 1, 0)
statusLabel.Position = UDim2.new(0, 0, 0, 0)
statusLabel.Text = "● OFF"
statusLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
statusLabel.BackgroundTransparency = 1
statusLabel.Font = Enum.Font.GothamBold
statusLabel.TextSize = 14
statusLabel.TextXAlignment = Enum.TextXAlignment.Left
statusLabel.Parent = statusRow

local speedLabel = Instance.new("TextLabel")
speedLabel.Size = UDim2.new(0.5, 0, 1, 0)
speedLabel.Position = UDim2.new(0.5, 0, 0, 0)
speedLabel.Text = "⚡ x7"
speedLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
speedLabel.BackgroundTransparency = 1
speedLabel.Font = Enum.Font.GothamBold
speedLabel.TextSize = 14
speedLabel.TextXAlignment = Enum.TextXAlignment.Right
speedLabel.Parent = statusRow

-- Main Buttons
local btnRow1 = Instance.new("Frame")
btnRow1.Size = UDim2.new(1, -20, 0, 40)
btnRow1.Position = UDim2.new(0, 10, 0, 40)
btnRow1.BackgroundTransparency = 1
btnRow1.Parent = contentFrame

-- START Button
local startBtn = Instance.new("TextButton")
startBtn.Size = UDim2.new(0.47, 0, 1, 0)
startBtn.Position = UDim2.new(0, 0, 0, 0)
startBtn.Text = "▶ START"
startBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
startBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 30)
startBtn.Font = Enum.Font.GothamBold
startBtn.TextSize = 14
startBtn.BorderSizePixel = 2
startBtn.BorderColor3 = Color3.fromRGB(255, 50, 50)
startBtn.Parent = btnRow1

local startCorner = Instance.new("UICorner")
startCorner.CornerRadius = UDim.new(0, 6)
startCorner.Parent = startBtn

-- BYPASS Button
local bypassBtn = Instance.new("TextButton")
bypassBtn.Size = UDim2.new(0.47, 0, 1, 0)
bypassBtn.Position = UDim2.new(0.53, 0, 0, 0)
bypassBtn.Text = "💀 BYPASS"
bypassBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
bypassBtn.BackgroundColor3 = Color3.fromRGB(130, 0, 30)
bypassBtn.Font = Enum.Font.GothamBold
bypassBtn.TextSize = 12
bypassBtn.BorderSizePixel = 2
bypassBtn.BorderColor3 = Color3.fromRGB(255, 50, 50)
bypassBtn.Parent = btnRow1

local bypassCorner = Instance.new("UICorner")
bypassCorner.CornerRadius = UDim.new(0, 6)
bypassCorner.Parent = bypassBtn

-- Button Row 2
local btnRow2 = Instance.new("Frame")
btnRow2.Size = UDim2.new(1, -20, 0, 40)
btnRow2.Position = UDim2.new(0, 10, 0, 85)
btnRow2.BackgroundTransparency = 1
btnRow2.Parent = contentFrame

-- UNEQUIP Button
local unequipBtn = Instance.new("TextButton")
unequipBtn.Size = UDim2.new(0.47, 0, 1, 0)
unequipBtn.Position = UDim2.new(0, 0, 0, 0)
unequipBtn.Text = "⏹ UNEQUIP"
unequipBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
unequipBtn.BackgroundColor3 = Color3.fromRGB(150, 50, 0)
unequipBtn.Font = Enum.Font.GothamBold
unequipBtn.TextSize = 12
unequipBtn.BorderSizePixel = 2
unequipBtn.BorderColor3 = Color3.fromRGB(255, 100, 0)
unequipBtn.Parent = btnRow2

local unequipCorner = Instance.new("UICorner")
unequipCorner.CornerRadius = UDim.new(0, 6)
unequipCorner.Parent = unequipBtn

-- AUTO UNEQUIP Button
local autoUnequipBtn = Instance.new("TextButton")
autoUnequipBtn.Size = UDim2.new(0.47, 0, 1, 0)
autoUnequipBtn.Position = UDim2.new(0.53, 0, 0, 0)
autoUnequipBtn.Text = "🔓 AUTO"
autoUnequipBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
autoUnequipBtn.BackgroundColor3 = Color3.fromRGB(100, 0, 30)
autoUnequipBtn.Font = Enum.Font.GothamBold
autoUnequipBtn.TextSize = 12
autoUnequipBtn.BorderSizePixel = 2
autoUnequipBtn.BorderColor3 = Color3.fromRGB(255, 50, 50)
autoUnequipBtn.Parent = btnRow2

local autoCorner = Instance.new("UICorner")
autoCorner.CornerRadius = UDim.new(0, 6)
autoCorner.Parent = autoUnequipBtn

-- Equip Label
local equipLabel = Instance.new("TextLabel")
equipLabel.Size = UDim2.new(1, -20, 0, 25)
equipLabel.Position = UDim2.new(0, 10, 0, 130)
equipLabel.Text = "EQUIP METHOD:"
equipLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
equipLabel.BackgroundTransparency = 1
equipLabel.Font = Enum.Font.GothamBold
equipLabel.TextSize = 12
equipLabel.TextXAlignment = Enum.TextXAlignment.Left
equipLabel.Parent = contentFrame

-- Equip Buttons
local equipRow = Instance.new("Frame")
equipRow.Size = UDim2.new(1, -20, 0, 40)
equipRow.Position = UDim2.new(0, 10, 0, 158)
equipRow.BackgroundTransparency = 1
equipRow.Parent = contentFrame

local equipBtns = {}
local equipIds = {5, 6, 7, 8}
for i, id in pairs(equipIds) do
    local btn = Instance.new("TextButton")
    local spacing = 0.22
    local startPos = 0.06
    btn.Size = UDim2.new(0.2, 0, 1, 0)
    btn.Position = UDim2.new(startPos + (i-1) * spacing, 0, 0, 0)
    btn.Text = tostring(id)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.BackgroundColor3 = Color3.fromRGB(80, 0, 30)
    btn.BackgroundTransparency = 0.2
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 16
    btn.BorderSizePixel = 2
    btn.BorderColor3 = Color3.fromRGB(200, 50, 50)
    btn.Parent = equipRow
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn
    
    equipBtns[id] = btn
end

-- Utility Buttons Row
local utilRow = Instance.new("Frame")
utilRow.Size = UDim2.new(1, -20, 0, 35)
utilRow.Position = UDim2.new(0, 10, 0, 203)
utilRow.BackgroundTransparency = 1
utilRow.Parent = contentFrame

-- REFRESH
local refreshBtn = Instance.new("TextButton")
refreshBtn.Size = UDim2.new(0.3, 0, 1, 0)
refreshBtn.Position = UDim2.new(0, 0, 0, 0)
refreshBtn.Text = "🔄 REF"
refreshBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
refreshBtn.BackgroundColor3 = Color3.fromRGB(80, 0, 80)
refreshBtn.Font = Enum.Font.GothamBold
refreshBtn.TextSize = 11
refreshBtn.BorderSizePixel = 2
refreshBtn.BorderColor3 = Color3.fromRGB(200, 50, 200)
refreshBtn.Parent = utilRow

local refreshCorner = Instance.new("UICorner")
refreshCorner.CornerRadius = UDim.new(0, 6)
refreshCorner.Parent = refreshBtn

-- MEMORY
local memBtn = Instance.new("TextButton")
memBtn.Size = UDim2.new(0.3, 0, 1, 0)
memBtn.Position = UDim2.new(0.35, 0, 0, 0)
memBtn.Text = "🧹 MEM"
memBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
memBtn.BackgroundColor3 = Color3.fromRGB(100, 0, 100)
memBtn.Font = Enum.Font.GothamBold
memBtn.TextSize = 11
memBtn.BorderSizePixel = 2
memBtn.BorderColor3 = Color3.fromRGB(200, 50, 200)
memBtn.Parent = utilRow

local memCorner = Instance.new("UICorner")
memCorner.CornerRadius = UDim.new(0, 6)
memCorner.Parent = memBtn

-- REJOIN
local rejoinBtn = Instance.new("TextButton")
rejoinBtn.Size = UDim2.new(0.3, 0, 1, 0)
rejoinBtn.Position = UDim2.new(0.7, 0, 0, 0)
rejoinBtn.Text = "🔄 REJOIN"
rejoinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
rejoinBtn.BackgroundColor3 = Color3.fromRGB(0, 50, 100)
rejoinBtn.Font = Enum.Font.GothamBold
rejoinBtn.TextSize = 10
rejoinBtn.BorderSizePixel = 2
rejoinBtn.BorderColor3 = Color3.fromRGB(50, 150, 255)
rejoinBtn.Parent = utilRow

local rejoinCorner = Instance.new("UICorner")
rejoinCorner.CornerRadius = UDim.new(0, 6)
rejoinCorner.Parent = rejoinBtn

-- LEAVE Button
local leaveBtn = Instance.new("TextButton")
leaveBtn.Size = UDim2.new(0.47, 0, 0, 35)
leaveBtn.Position = UDim2.new(0.265, 0, 0, 243)
leaveBtn.Text = "🚪 LEAVE"
leaveBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
leaveBtn.BackgroundColor3 = Color3.fromRGB(100, 0, 0)
leaveBtn.Font = Enum.Font.GothamBold
leaveBtn.TextSize = 12
leaveBtn.BorderSizePixel = 2
leaveBtn.BorderColor3 = Color3.fromRGB(255, 50, 50)
leaveBtn.Parent = contentFrame

local leaveCorner = Instance.new("UICorner")
leaveCorner.CornerRadius = UDim.new(0, 6)
leaveCorner.Parent = leaveBtn

-- Footer
local footer = Instance.new("Frame")
footer.Size = UDim2.new(1, 0, 0, 25)
footer.Position = UDim2.new(0, 0, 0, 283)
footer.BackgroundColor3 = Color3.fromRGB(200, 0, 30)
footer.BackgroundTransparency = 0.15
footer.BorderSizePixel = 0
footer.Parent = contentFrame

local footerLabel = Instance.new("TextLabel")
footerLabel.Size = UDim2.new(1, 0, 1, 0)
footerLabel.Text = "✦ LilyVRedHawk • Demon Arena ✦"
footerLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
footerLabel.BackgroundTransparency = 1
footerLabel.Font = Enum.Font.Gotham
footerLabel.TextSize = 11
footerLabel.Parent = footer

-- ========== MINIMIZE FUNCTION ==========
local isMinimized = false
local originalSize = mainFrame.Size

minBtn.MouseButton1Click:Connect(function()
    isMinimized = not isMinimized
    
    if isMinimized then
        minBtn.Text = "□"
        minBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 50)
        minBtn.BorderColor3 = Color3.fromRGB(50, 255, 50)
        
        local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
        local targetSize = UDim2.new(0, 280, 0, 45)
        local tween = tweenService:Create(mainFrame, tweenInfo, {Size = targetSize})
        tween:Play()
        
        contentFrame.Visible = false
    else
        minBtn.Text = "─"
        minBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 30)
        minBtn.BorderColor3 = Color3.fromRGB(255, 50, 50)
        
        contentFrame.Visible = true
        local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
        local tween = tweenService:Create(mainFrame, tweenInfo, {Size = originalSize})
        tween:Play()
    end
end)

-- ========== DRAG UNTUK MOBILE & PC ==========
local dragging = false
local dragStartPos = nil
local dragStartMousePos = nil

local function startDrag(input)
    dragging = true
    dragStartPos = mainFrame.Position
    dragStartMousePos = input.Position
end

local function updateDrag(input)
    if dragging and dragStartPos and dragStartMousePos then
        local delta = input.Position - dragStartMousePos
        mainFrame.Position = UDim2.new(
            dragStartPos.X.Scale,
            dragStartPos.X.Offset + delta.X,
            dragStartPos.Y.Scale,
            dragStartPos.Y.Offset + delta.Y
        )
    end
end

local function endDrag()
    dragging = false
    dragStartPos = nil
    dragStartMousePos = nil
end

-- Untuk PC (Mouse)
header.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        startDrag(input)
    end
end)

header.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        updateDrag(input)
    end
end)

userInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        endDrag()
    end
end)

-- Untuk Mobile (Touch)
header.TouchBegan:Connect(function(input)
    startDrag(input)
end)

header.TouchMoved:Connect(function(input)
    updateDrag(input)
end)

header.TouchEnded:Connect(function()
    endDrag()
end)

-- ========== FUNGSI BUTTON ==========
local active = false
local autoUnequipEnabled = true

startBtn.MouseButton1Click:Connect(function()
    active = not active
    if active then
        startLoop()
        startBtn.Text = "⏹ STOP"
        startBtn.BackgroundColor3 = Color3.fromRGB(100, 0, 20)
        statusLabel.Text = "● ON"
        statusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
    else
        stopLoop()
        startBtn.Text = "▶ START"
        startBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 30)
        statusLabel.Text = "● OFF"
        statusLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
    end
end)

bypassBtn.MouseButton1Click:Connect(function()
    bypassActive = not bypassActive
    if bypassActive then
        activateBypass(currentSpeed)
        bypassBtn.Text = "💀 ON"
        bypassBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 30)
    else
        deactivateBypass()
        bypassBtn.Text = "💀 BYPASS"
        bypassBtn.BackgroundColor3 = Color3.fromRGB(130, 0, 30)
    end
end)

unequipBtn.MouseButton1Click:Connect(function()
    unequipTrain()
end)

for id, btn in pairs(equipBtns) do
    btn.MouseButton1Click:Connect(function()
        cachedEquip(id)
        for _, b in pairs(equipBtns) do
            b.BackgroundTransparency = 0.2
            b.BorderColor3 = Color3.fromRGB(200, 50, 50)
            b.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
        btn.BackgroundTransparency = 0.1
        btn.BorderColor3 = Color3.fromRGB(255, 100, 100)
        btn.TextColor3 = Color3.fromRGB(255, 200, 200)
    end)
end

refreshBtn.MouseButton1Click:Connect(function()
    statusLabel.Text = "● OK"
    statusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
    task.wait(0.2)
    if active then
        statusLabel.Text = "● ON"
        statusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
    else
        statusLabel.Text = "● OFF"
        statusLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
    end
end)

memBtn.MouseButton1Click:Connect(function()
    quickMemoryClean()
    statusLabel.Text = "● CLEAN"
    statusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
    task.wait(0.3)
    if active then
        statusLabel.Text = "● ON"
        statusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
    else
        statusLabel.Text = "● OFF"
        statusLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
    end
end)

autoUnequipBtn.MouseButton1Click:Connect(function()
    autoUnequipEnabled = not autoUnequipEnabled
    if autoUnequipEnabled then
        autoUnequipBtn.Text = "🔓 AUTO"
        autoUnequipBtn.BackgroundColor3 = Color3.fromRGB(100, 0, 30)
        autoUnequipBtn.BorderColor3 = Color3.fromRGB(255, 50, 50)
        setupAutoUnequip()
    else
        autoUnequipBtn.Text = "🔒 AUTO"
        autoUnequipBtn.BackgroundColor3 = Color3.fromRGB(30, 0, 10)
        autoUnequipBtn.BorderColor3 = Color3.fromRGB(100, 30, 30)
        autoUnequipConnected = false
    end
end)

leaveBtn.MouseButton1Click:Connect(function()
    stopLoop()
    deactivateBypass()
    task.wait(0.3)
    player:Kick("Leave")
end)

rejoinBtn.MouseButton1Click:Connect(function()
    stopLoop()
    deactivateBypass()
    task.wait(0.3)
    game:GetService("TeleportService"):Teleport(game.PlaceId, player)
end)

setupAutoUnequip()
